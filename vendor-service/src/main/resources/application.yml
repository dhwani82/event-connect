server:
  port: 8082

spring:
  application:
    name: vendor-service
  datasource:
    url: jdbc:postgresql://postgres:5432/vendors_db?currentSchema=public
    username: postgres
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
